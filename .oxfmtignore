# Ignore JSON files (oxfmt has issues with JSON)
**/*.json

# Ignore Markdown files (oxfmt has issues with some Markdown)
**/*.md

# Ignore fixture files
tests/fixtures

# Ignore build outputs
_build
_release
_temp
dist
node_modules
